{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.1"
  },
  "scripts": {
    "start": "lerna run start --scope={web-client,server-api}",
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "learna diff"
  }
}
